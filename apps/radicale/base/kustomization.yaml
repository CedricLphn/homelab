apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: radicale

labels:
  - pairs:
      app: radicale

resources:
  - namespace.yaml
  - pvc.yaml
  - configmap.yaml
  - external-secrets.yaml
  - deployment.yaml
  - service.yaml
  - tailscale-ingress.yaml
